import { useEffect, useState } from "react";
import { useRecoilValueLoadable } from "recoil";

/** */
export default function WindowStatusBar() {
  return (
    <footer id={"app-mainfooter"}>
    </footer>
  );
}
